# Soul Sense EQ Test Requirements
# This project uses Python built-in libraries with analytics dashboard

# Core dependencies (built-in):
# - tkinter (GUI framework with ttk for tabs)
# - sqlite3 (database)
# - datetime (date/time handling)
# - re (regular expressions)
# - collections (Counter for analytics)

# External packages for visualization
matplotlib>=3.5.0

# Python 3.6+ recommended